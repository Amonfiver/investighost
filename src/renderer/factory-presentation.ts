export const jobStatusLabel = (value: string): string => ({ QUEUED: 'En cola', PROCESSING: 'Procesando', READY_FOR_REVIEW: 'Para revisar', APPROVED: 'Aprobado', REDO_REQUIRED: 'Rehacer', FAILED: 'Fallido', BLOCKED_AMBIGUOUS: 'Requiere revisión', REUSED: 'Reutilizado', DELIVERED: 'Enviado', IMPORTED: 'Importado' } as Record<string, string>)[value] ?? value
export const jobPhaseLabel = (value: string): string => ({ IDENTITY: 'Preparando destino', RESEARCH: 'Investigando', ANALYSIS: 'Analizando', STUDENT: 'Creando Student', ADVENTURE: 'Creando Adventure', VISUALS: 'Buscando imágenes', AUTO_REVIEW: 'Revisando automáticamente', DELIVERY: 'Enviando' } as Record<string, string>)[value] ?? value
export const formatCost = (value: number): string => new Intl.NumberFormat('es-ES', { style: 'currency', currency: 'EUR' }).format(value)
export function validateBatchFile(file: File): string | null { if (!file.name.toLowerCase().endsWith('.json')) return 'Archivo no válido. Selecciona un archivo de lote (.json).'; if (file.type && !['application/json', 'text/json'].includes(file.type)) return 'Archivo no válido. Selecciona un archivo de lote (.json).'; return null }
